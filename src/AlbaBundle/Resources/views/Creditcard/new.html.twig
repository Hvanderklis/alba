{% extends '@Alba/adminpanel.html.twig' %}

{% block body %}
    <h1>Creditcard creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="button" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('card_index') }}" class="button">Back to the list</a>
        </li>
    </ul>
{% endblock %}
