{% extends 'AlbaBundle::base.html.twig' %}
{% block body %}

    {% set reserveren = app.session.get('reserveren') %}
    <h1>Rentable Rooms</h1>
    <h2>Welcome {{ reserveren.Firstname }}</h2>
    {% for kamer in kamers %}
        <table>

        <div class="row small-up-1 medium-up-2 large-up-3">
        <div class="column">
        <div class="callout" id="book">
        <p id="colorw">{{ kamer.kamerNaam }}</p>
        <p><img src="{{ asset('bundles/alba/img/single.jpg') }}" alt="Sigle Room"></p>
        <p class="lead" id="colorw">{{ kamer.omschrijving }}</p>
        <p class="subheader" id="colorw">€ {{ kamer.prijs }}</p>
        <a class="button" href="{{ path('kamer_reserveren_show', { 'id': kamer.id }) }}">Info</a>
    {% endfor %}
    </div>
    </div>
    </div>
    <div class="row">
    {% for kamer in kamer %}

        <div class="small-12 medium-6 large-6 column">
            <div class="callout" id="book">
                <p id="colorw">{{ kamer.kamerNaam }}</p>
                {% for kamerafbeelding in kamerafbeelding %}
                    {% if kamerafbeelding.kamer.id == kamer.id %}
                        <img id="roomIm" src="/{{ kamerafbeelding.path}}/{{  kamerafbeelding.name }}.{{ kamerafbeelding.type }}" alt="{{ kamer.kamerNaam }}">
                    {% endif %}
                {% endfor %}
                <p id="colorw" style="padding-top: 20px">{{ kamer.omschrijving }}</p>
                <p class="subheader" id="colorw">&pound; {{ kamer.prijs }}</p>
                <a class="button" href="{{ path('kamer_reserveren_show', { 'id': kamer.id }) }}">Info</a>
            </div>
        </div>

    {% endfor %}
    </div>
{% endblock %}