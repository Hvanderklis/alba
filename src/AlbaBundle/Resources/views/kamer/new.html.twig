{% extends 'AlbaBundle::adminpanel.html.twig' %}

{% block body %}
    <h1>Kamer creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="button success" style="float: right" type="submit" value="Create" />
    {{ form_end(form) }}

    <a class="bsuccess button" href="{{ path('kamer_index') }}">Back to the list</a>

{% endblock %}
