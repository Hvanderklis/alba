{% extends 'AlbaBundle::adminpanel.html.twig' %}

{% block body %}
    <h1>Registration</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('kamer_reserveren') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}